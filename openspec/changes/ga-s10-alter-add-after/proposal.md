## Why

MySQL 兼容性需要 ALTER TABLE ADD COLUMN 支持 AFTER/FIRST 位置控制。

## What Changes

- 新增 `docs/ALTER_ADD_COLUMN_AFTER.md` — 设计文档

## Capabilities

### New Capabilities

- `alter-add-after`: ALTER TABLE ADD COLUMN AFTER/FIRST 位置控制
