# Tarpaulin configuration for test coverage
# Excludes benchmarks and examples from coverage

[tarpaulin]
output-dir = "target/tarpaulin"
all = true
clean = true
timeout = "5m"
exclude-files = [
    "benches/*",
    "examples/*",
]
