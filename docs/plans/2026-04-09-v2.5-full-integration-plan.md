# v2.5 开发计划：全面集成 + GMP内审 + OpenClaw调度

## 版本目标

整合 SQLRustGo v2.1-v2.5 所有功能模块，实现企业级 GMP 内审支持和 OpenClaw AI Agent 全自动调度。

## 版本号

- **版本**: v2.5.0
- **代号**: Full Integration
- **目标日期**: 待定

---

## 1. 全模块整合

### 1.1 统一查询 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/v2/query/unified` | POST | SQL + Vector + Graph 联合查询 |
| `/api/v2/query/vector` | POST | 纯向量检索 |
| `/api/v2/query/graph` | POST | 纯图查询 |

#### Unified Query Request

```json
{
  "query": "最近批准的GMP清洁验证SOP相关设备依赖",
  "mode": "sql_vector_graph",
  "filters": {
    "date_range": {"start": "2026-01-01", "end": "2026-12-31"},
    "department": "生产部"
  },
  "top_k": 10
}
```

### 1.2 统一存储层

- [ ] 统一 Document 表设计
- [ ] 向量列式存储优化
- [ ] 图索引与 SQL 索引联动

### 1.3 统一优化器

- [ ] Cost-based optimization for hybrid queries
- [ ] 自动选择执行路径
- [ ] 查询计划可视化

---

## 2. OpenClaw 全局调度

### 2.1 任务流编排 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/v2/scheduler/task` | POST | 创建任务 |
| `/api/v2/scheduler/task/{id}` | GET | 查询任务状态 |
| `/api/v2/scheduler/workflow` | POST | 创建工作流 |
| `/api/v2/scheduler/execute` | POST | 执行工作流 |

### 2.2 工作流定义

```json
{
  "name": "GMP_SOP_Approval_Workflow",
  "steps": [
    {"agent": "reviewer", "action": "review", "timeout": 3600},
    {"agent": "approver", "action": "approve", "timeout": 7200},
    {"agent": "notifier", "action": "notify", "timeout": 300}
  ],
  "on_failure": "retry",
  "max_retries": 3
}
```

### 2.3 Agent 间协作

- [ ] 多 Agent 任务分发
- [ ] Agent 状态同步
- [ ] 结果聚合

---

## 3. GMP 内审支持

### 3.1 审计日志表

```sql
CREATE TABLE gmp_audit_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    user_id VARCHAR(100) NOT NULL,
    action VARCHAR(50) NOT NULL,
    table_name VARCHAR(100) NOT NULL,
    record_id VARCHAR(100),
    old_value JSON,
    new_value JSON,
    ip_address VARCHAR(50),
    session_id VARCHAR(100),
    checksum VARCHAR(64) NOT NULL,
    INDEX idx_timestamp (timestamp),
    INDEX idx_user_id (user_id),
    INDEX idx_table_name (table_name)
);
```

### 3.2 GMP 报表 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/v2/gmp/report/audit` | GET | 审计报表 |
| `/api/v2/gmp/report/deviation` | GET | 偏差报表 |
| `/api/v2/gmp/report/capa` | GET | CAPA 报表 |
| `/api/v2/gmp/compliance/check` | POST | 合规检查 |

### 3.3 报表格式

```json
{
  "report_type": "audit",
  "period": {"start": "2026-01-01", "end": "2026-03-31"},
  "total_records": 15420,
  "by_action": {
    "CREATE": 5230,
    "UPDATE": 8920,
    "DELETE": 1270
  },
  "by_user": [...],
  "export_url": "/api/v2/gmp/report/audit/export/pdf"
}
```

---

## 4. 全面性能验证

### 4.1 TPC-H 测试矩阵

| 规模 | 数据量 | 目标 Q1 延迟 |
|------|--------|---------------|
| SF=0.1 | 600K rows | < 50ms |
| SF=1 | 6M rows | < 100ms |
| SF=10 | 60M rows | < 500ms |
| SF=100 | 600M rows | < 2000ms |

### 4.2 向量检索基准

| 数据规模 | KNN 延迟目标 |
|----------|---------------|
| 10K vectors | < 5ms |
| 100K vectors | < 20ms |
| 1M vectors | < 100ms |

### 4.3 图查询基准

| 查询类型 | 延迟目标 |
|----------|----------|
| BFS 路径 | < 50ms |
| DFS 子图 | < 100ms |
| 多跳查询 | < 500ms |

---

## 5. 技术架构

### 5.1 模块依赖

```
┌─────────────────────────────────────────────────┐
│ 用户/Agent (OpenClaw + LLM)                    │
└────────────────────┬──────────────────────────┘
                     │
┌────────────────────▼──────────────────────────┐
│ API Gateway                                  │
│  /api/v2/query/*  /api/v2/scheduler/*       │
│  /api/v2/gmp/*                               │
└────────────────────┬──────────────────────────┘
                     │
    ┌────────────────┼────────────────┐
    ▼                ▼                ▼
┌────────┐    ┌──────────┐    ┌────────┐
│ SQL    │    │ Vector   │    │ Graph  │
│ Engine│    │ Engine   │    │ Engine │
└────────┘    └──────────┘    └────────┘
    │                │                │
    └────────────────┼────────────────┘
                     ▼
            ┌─────────────────┐
            │ Unified Storage  │
            │ (Columnar + mmap) │
            └─────────────────┘
```

### 5.2 数据库表设计

```sql
-- 统一文档表
CREATE TABLE unified_documents (
    id BIGINT PRIMARY KEY,
    doc_type ENUM('sql', 'vector', 'graph', 'gmp') NOT NULL,
    title VARCHAR(500),
    content TEXT,
    metadata JSON,
    embedding VECTOR(1536),
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    INDEX idx_doc_type (doc_type),
    VECTOR INDEX idx_embedding (embedding)
);

-- GMP 审计日志
CREATE TABLE gmp_audit_log (...);

-- 工作流定义
CREATE TABLE workflow_definitions (
    id BIGINT PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    definition JSON NOT NULL,
    version INT DEFAULT 1,
    created_at TIMESTAMP,
    INDEX idx_name (name)
);

-- 工作流执行记录
CREATE TABLE workflow_executions (
    id BIGINT PRIMARY KEY,
    workflow_id BIGINT NOT NULL,
    status ENUM('pending', 'running', 'completed', 'failed') NOT NULL,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    result JSON,
    INDEX idx_workflow_id (workflow_id),
    INDEX idx_status (status)
);
```

---

## 6. 开发任务分解

### 6.1 任务分配

| 模块 | 任务数 | 预估工时 |
|------|--------|----------|
| 统一查询 API | 8 | 2 周 |
| OpenClaw 调度 | 12 | 3 周 |
| GMP 内审 | 10 | 2 周 |
| TPC-H 测试 | 6 | 1 周 |
| 集成测试 | 4 | 1 周 |
| **总计** | **40** | **9 周** |

### 6.2 Sprint 规划

#### Sprint 1: 统一查询 API (2 周)
- [ ] Unified query endpoint
- [ ] Result fusion
- [ ] Query optimizer extension

#### Sprint 2-3: OpenClaw 调度 (3 周)
- [ ] Task/Workflow CRUD
- [ ] Workflow execution engine
- [ ] Multi-agent coordination

#### Sprint 4: GMP 内审 (2 周)
- [ ] Audit log table & API
- [ ] Compliance check engine
- [ ] Report generation

#### Sprint 5: 性能测试 (1 周)
- [ ] TPC-H SF=1/10/100
- [ ] Vector benchmark
- [ ] Graph benchmark

#### Sprint 6: 集成测试 (1 周)
- [ ] End-to-end tests
- [ ] Performance regression tests
- [ ] Documentation

---

## 7. 验收标准

### 功能验收

- [ ] 统一查询 API 支持三种模式
- [ ] OpenClaw 工作流正常执行
- [ ] GMP 审计日志完整
- [ ] 报表正确生成

### 性能验收

- [ ] TPC-H SF=1 Q1 < 100ms
- [ ] 向量 10K < 5ms
- [ ] 图遍历 1000 节点 < 100ms

### 合规验收

- [ ] 审计日志不可篡改
- [ ] 100% 操作可追溯
- [ ] 自查报告自动生成

---

## 8. 里程碑

| 里程碑 | 日期 | 交付内容 |
|--------|------|----------|
| M1: 统一查询 API | Week 2 | 混合查询支持 |
| M2: OpenClaw 调度 | Week 5 | 工作流引擎 |
| M3: GMP 内审 | Week 7 | 审计报表 |
| M4: 性能验证 | Week 8 | TPC-H 报告 |
| M5: v2.5.0 GA | Week 9 | 正式发布 |

---

*文档创建: 2026-04-09*
*Issue: #1326*
