# v1.9.0 测试计划

> **版本**: v1.9.0
> **更新日期**: 2026-03-26
> **状态**: 开发中

---

## 1. 测试概述

本文档详细记录 v1.9.0 所有功能的测试计划，包括功能测试、压力测试、崩溃恢复测试等。

---

## 2. 功能与测试映射

### 2.1 SQL 核心功能

#### 2.1.1 子查询增强 (ISSUE #907)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| ScalarSubquery | P0 | 单值查询验证 | `SELECT * FROM t1 WHERE col = (SELECT MAX(x) FROM t2)` | ✅ 已完成 |
| IN Subquery | P0 | 多值查询验证 | `SELECT * FROM t1 WHERE id IN (SELECT id FROM t2)` | ✅ 已完成 |
| EXISTS | P0 | 存在性查询验证 | `SELECT * FROM t1 WHERE EXISTS (SELECT 1 FROM t2 WHERE t2.id = t1.id)` | ✅ 已完成 |
| ANY/ALL | P1 | 与比较运算结合测试 | `SELECT * FROM t1 WHERE col > ALL (SELECT x FROM t2)` | ✅ 已完成 |

**测试验证项**:
- [x] scalar subquery 返回单值
- [x] IN subquery 多值匹配
- [x] EXISTS 存在性检查
- [x] ANY/ALL 比较运算
- [ ] 嵌套子查询
- [ ] 子查询与 JOIN 组合

#### 2.1.2 JOIN 优化 (ISSUE #905)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| INNER JOIN | P0 | 多表连接正确性 | `SELECT * FROM t1 INNER JOIN t2 ON t1.id = t2.id` | 🔶 待测试 |
| LEFT JOIN | P0 | 左表保留 + 性能 | `SELECT * FROM t1 LEFT JOIN t2 ON t1.id = t2.id` | 🔶 待测试 |
| RIGHT JOIN | P0 | 右表保留 | `SELECT * FROM t1 RIGHT JOIN t2 ON t1.id = t2.id` | ⬜ 待实现 |
| CROSS JOIN | P0 | 笛卡尔积 | `SELECT * FROM t1 CROSS JOIN t2` | ⬜ 待实现 |

**测试验证项**:
- [ ] INNER JOIN 结果正确性
- [ ] LEFT JOIN NULL 填充
- [ ] RIGHT JOIN 结果正确性
- [ ] CROSS JOIN 笛卡尔积
- [ ] 多表 JOIN
- [ ] JOIN 与子查询组合

#### 2.1.3 窗口函数

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| ROW_NUMBER() | P1 | 分区编号 | `SELECT ROW_NUMBER() OVER (PARTITION BY col ORDER BY id)` | ⬜ 待实现 |
| SUM() OVER | P1 | 分区求和 | `SELECT SUM(col) OVER (PARTITION BY type)` | ⬜ 待实现 |
| AVG() OVER | P1 | 分区平均 | `SELECT AVG(col) OVER (PARTITION BY type)` | ⬜ 待实现 |
| PARTITION BY | P1 | 数据分区 | `SELECT *, SUM(x) OVER (PARTITION BY y)` | ⬜ 待实现 |

---

### 2.2 数据完整性

#### 2.2.1 外键约束 (ISSUE #901)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 单表外键 | P0 | 插入/删除验证 | `FOREIGN KEY (pid) REFERENCES parent(id)` | ⬜ 待实现 |
| 多表外键 | P0 | 级联更新/删除 | `ON DELETE CASCADE` | ⬜ 待实现 |
| 外键约束检查 | P0 | 违反约束报错 | 插入不存在父记录 | ⬜ 待实现 |

#### 2.2.2 AUTO_INCREMENT (ISSUE #902)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 自动序列 | P0 | 唯一性验证 | `INSERT INTO t VALUES (NULL, 'x')` | ⬜ 待实现 |
| LAST_INSERT_ID | P0 | 获取最后插入ID | `SELECT LAST_INSERT_ID()` | ⬜ 待实现 |

#### 2.2.3 UPSERT (ISSUE #903)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| INSERT ON DUPLICATE KEY UPDATE | P0 | 唯一性验证 | `INSERT ... ON DUPLICATE KEY UPDATE` | ⬜ 待实现 |
| REPLACE | P0 | 插入或替换 | `REPLACE INTO t VALUES (...)` | ⬜ 待实现 |

---

### 2.3 事务增强 (ISSUE #918)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| SAVEPOINT | P0 | 事务保存点 | `SAVEPOINT sp1;` | ⬜ 待实现 |
| ROLLBACK TO SAVEPOINT | P0 | 回滚到保存点 | `ROLLBACK TO SAVEPOINT sp1;` | ⬜ 待实现 |
| 多事务并发 | P0 | 并发验证 | 50+ 并发事务 | ⬜ 待实现 |

---

### 2.4 性能优化

#### 2.4.1 批量写入 (ISSUE #904)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 批量 INSERT | P1 | 大数据量性能 | `INSERT INTO t VALUES (...), (...), (...)` | ✅ 已完成 |
| 批量 UPSERT | P1 | 批量更新 | `INSERT ... ON DUPLICATE KEY UPDATE` | ⬜ 待实现 |

**性能目标**:
- 单次批量插入: 10000+ rows/s
- 单次插入 1000 行: < 100ms

#### 2.4.2 查询缓存 (ISSUE #909)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 缓存命中 | P1 | SELECT 性能验证 | 相同查询二次执行 | ⬜ 待实现 |
| 缓存失效 | P1 | 数据变更后失效 | INSERT/UPDATE/DELETE 后 | ⬜ 待实现 |

#### 2.4.3 连接池 (ISSUE #908)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 连接复用 | P0 | 并发测试 | 50+ 并发连接 | ⬜ 待实现 |
| 连接超时 | P0 | 超时处理 | 连接超时配置 | ⬜ 待实现 |

---

### 2.5 运维功能

#### 2.5.1 数据备份导出 (ISSUE #911)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| CSV 导出 | P0 | 导出后可重导入验证 | `BACKUP TABLE t TO 'file.csv' FORMAT CSV` | ✅ 已完成 |
| JSON 导出 | P0 | JSON 格式验证 | `BACKUP TABLE t TO 'file.json' FORMAT JSON` | ✅ 已完成 |
| SQL 导出 | P0 | SQL INSERT 语句 | `BACKUP TABLE t TO 'file.sql' FORMAT SQL` | ✅ 已完成 |

**测试验证项**:
- [x] CSV 格式正确
- [x] JSON 格式正确
- [x] SQL INSERT 语句格式正确
- [x] 导出后数据可恢复
- [ ] 大数据量导出性能

#### 2.5.2 数据恢复 (ISSUE #912)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| 完整恢复 | P0 | 崩溃后数据一致性 | `RESTORE FROM 'backup.file'` | ✅ 已完成 |
| 部分恢复 | P0 | 指定表恢复 | `RESTORE TABLE t FROM 'backup.file'` | 🔶 待完善 |

#### 2.5.3 崩溃恢复测试 (ISSUE #913)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| kill 进程 | P0 | 数据完整性 | kill -9 模拟断电 | ✅ 已完成 |
| 异常中断 | P0 | WAL 恢复 | 事务中断恢复 | ✅ 已完成 |
| 部分损坏 | P0 | 数据修复 | WAL 日志损坏 | ✅ 已完成 |

#### 2.5.4 生产场景测试 (ISSUE #914)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| OLTP 负载 | P0 | 小型生产负载验证 | 订单/库存场景 | ✅ 已完成 |
| 并发读写 | P0 | 混合工作负载 | 50+ 并发读写 | ✅ 已完成 |

#### 2.5.5 日志与监控

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| SQL 执行日志 | P1 | 日志完整性 | 记录所有 SQL | ⬜ 待实现 |
| 错误日志 | P1 | 错误可追溯 | ERROR 级别日志 | ⬜ 待实现 |
| 执行计划 | P1 | EXPLAIN 输出 | `EXPLAIN SELECT ...` | ⬜ 待实现 |

#### 2.5.6 权限管理

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| READ 权限 | P2 | 只读用户验证 | SELECT 可用，INSERT 拒绝 | ⬜ 待实现 |
| WRITE 权限 | P2 | 读写用户验证 | INSERT/UPDATE 可用 | ⬜ 待实现 |

---

## 3. 索引支持 (ISSUE #910)

| 子功能 | 优先级 | 测试需求 | 测试用例 | 状态 |
|--------|--------|----------|----------|------|
| B+ Tree 索引 | P1 | SELECT/WHERE 加速 | `CREATE INDEX ON t(col); SELECT * FROM t WHERE col = x` | 🔶 基础 |
| Hash 索引 | P1 | 等值查询加速 | `CREATE HASH INDEX ON t(col)` | ⬜ 待实现 |
| 索引选择 | P1 | 查询计划使用索引 | `EXPLAIN` 查看索引使用 | ⬜ 待实现 |

---

## 4. 测试类型与覆盖率

### 4.1 功能测试

| 模块 | 测试用例数 | 目标 | 当前状态 |
|------|------------|------|----------|
| Parser | 150+ | 覆盖所有语法 | ✅ |
| Executor | 100+ | 覆盖所有算子 | 🔶 70% |
| Storage | 280+ | 覆盖存储引擎 | ✅ |
| Planner | 320+ | 覆盖所有计划节点 | ✅ |
| Transaction | 50+ | 覆盖事务场景 | 🔶 60% |

### 4.2 压力测试

| 测试项 | 并发数 | 目标 QPS | 当前状态 |
|--------|--------|----------|----------|
| 并发连接 | 50+ | 1000+ | ⬜ 待测试 |
| 批量插入 | 10 线程 | 10000+ rows/s | ⬜ 待测试 |
| 复杂查询 | 20 并发 | < 100ms | ⬜ 待测试 |
| 混合负载 | 50:50 读写 | 稳定运行 | ⬜ 待测试 |

### 4.3 崩溃恢复测试

| 测试场景 | 恢复时间目标 | 当前状态 |
|----------|--------------|----------|
| kill -9 断电 | < 5s | ✅ 已完成 |
| WAL 日志损坏 | 数据完整 | ✅ 已完成 |
| 部分写入中断 | 无数据丢失 | ✅ 已完成 |

---

## 5. 优先级矩阵

| 优先级 | 功能 | 测试阶段 | 截止日期 |
|--------|------|----------|----------|
| P0 | 子查询增强 | Alpha | 已完成 |
| P0 | JOIN 优化 | Alpha | 2026-03-30 |
| P0 | 外键约束 | Beta | 2026-04-05 |
| P0 | 事务增强 (SAVEPOINT) | Beta | 2026-04-05 |
| P0 | AUTO_INCREMENT/UPSERT | Beta | 2026-04-07 |
| P0 | 批量写入优化 | Beta | 已完成 |
| P0 | 连接池 | RC | 2026-04-10 |
| P0 | 数据备份/恢复 | Alpha | 已完成 |
| P0 | 崩溃恢复测试 | Alpha | 已完成 |
| P0 | 生产场景测试 | Alpha | 已完成 |
| P1 | 查询缓存 | RC | 2026-04-12 |
| P1 | 窗口函数 | RC | 2026-04-14 |
| P1 | 索引优化 | RC | 2026-04-15 |
| P1 | 日志与监控 | RC | 2026-04-15 |
| P2 | 权限管理 | GA | 待定 |

---

## 6. 测试执行计划

### Phase 1: Alpha (已完成)

- [x] 子查询增强测试
- [x] 数据备份导出测试
- [x] 数据恢复测试
- [x] 崩溃恢复测试
- [x] 生产场景测试

### Phase 2: Beta (进行中)

- [ ] JOIN 优化测试
- [ ] 外键约束测试
- [ ] 事务增强测试
- [ ] AUTO_INCREMENT/UPSERT 测试

### Phase 3: RC

- [ ] 连接池压力测试
- [ ] 查询缓存测试
- [ ] 窗口函数测试
- [ ] 索引性能测试

### Phase 4: GA

- [ ] 完整回归测试
- [ ] 性能基准测试
- [ ] 文档完整性检查

---

## 7. 测试环境要求

| 环境 | 配置 | 用途 |
|------|------|------|
| 开发环境 | macOS, 16GB RAM | 功能开发 |
| CI 环境 | Linux, 8GB RAM | 自动化测试 |
| 压力测试环境 | Linux, 32GB RAM, 8+ CPU | 性能测试 |
| 生产模拟环境 | Linux, 64GB RAM | 预发布验证 |

---

## 8. 测试通过标准

| 测试类型 | 通过标准 |
|----------|----------|
| 单元测试 | 100% 通过 |
| 集成测试 | 100% 通过 |
| SQL-92 测试 | 100% 通过 |
| 压力测试 | QPS ≥ 1000, 并发 ≥ 50 |
| 崩溃恢复 | 0 数据丢失 |
| 覆盖率 | ≥ 75% (RC), ≥ 80% (GA) |

---

## 9. 图例

| 符号 | 含义 |
|------|------|
| ✅ | 已完成且通过 |
| 🔶 | 已完成但待测试或部分完成 |
| ⬜ | 待实现/待测试 |

---

*本文档由 OpenClaw AI 生成*
*更新日期: 2026-03-26*
*版本: v1.9.0*
