# SQLRustGo v1.6.0 模块任务清单

## 一、模块状态总览

| 模块 | 子模块 | 功能 | 状态 | 负责人 | PR |
|------|--------|------|------|--------|-----|
| **Transaction** | MVCC | 多版本并发控制 | ✅ 完成 | - | - |
| | TxManager | 事务管理 | ✅ 完成 | - | - |
| | LockManager | 行级锁 | ✅ 完成 | - | PR #633 |
| | Deadlock | 死锁检测 | ⏳ 待做 | - | PR #628 |
| **Storage** | B+Tree | 索引结构 | ✅ 完成 | - | - |
| | WAL | 预写日志 | ✅ 完成 | - | - |
| | BufferPool | 缓冲池 | ✅ 完成 | - | - |
| **Index** | 唯一索引 | UNIQUE | ✅ 完成 | - | - |
| | 复合索引 | 多列索引 | ✅ 完成 | - | - |
| | 索引统计 | 成本估算 | ✅ 完成 | - | - |
| **Performance** | QueryCache | 查询缓存 | ✅ 完成 | - | PR #627 |
| | ConnectionPool | 连接池 | ⏳ 待做 | - | PR #630 |
| | TPC-H | Q1/Q6 | ⏳ 待做 | - | PR #631 |
| **Types** | Integer | 整数 | ✅ 完成 | - | - |
| | Float | 浮点 | ✅ 完成 | - | - |
| | Varchar | 字符串 | ✅ 完成 | - | - |
| | Date | 日期 | ✅ 完成 | - | PR #624 |
| | Timestamp | 时间戳 | ✅ 完成 | - | PR #634 |
| **Planner** | Parser | 解析器 | ✅ 完成 | - | - |
| | LogicalPlan | 逻辑计划 | ✅ 完成 | - | - |
| | PhysicalPlan | 物理计划 | ✅ 完成 | - | - |
| **Optimizer** | Rules | 规则优化 | ✅ 完成 | - | - |
| | CBO | 成本优化 | ✅ 完成 | - | - |

---

## 二、详细任务清单

### T-04 行级锁 (LockManager)

```rust
struct LockManager {
    locks: DashMap<RowId, LockState>,
}

struct LockState {
    owners: Vec<TxId>,
    waiters: Vec<TxId>,
    lock_type: LockType,
}

enum LockType {
    Shared,
    Exclusive,
}
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 共享锁可并发获取 | ✅ | 并发测试 |
| 排他锁互斥 | ✅ | 互斥测试 |
| 锁释放唤醒等待者 | ✅ | 唤醒测试 |
| 锁超时处理 | ✅ | 超时测试 |

**文件位置**: `crates/transaction/src/lock.rs`
**测试文件**: `crates/transaction/tests/lock_test.rs`

---

### T-05 死锁检测 (Deadlock Detection)

```rust
struct WaitForGraph {
    edges: HashMap<TxId, HashSet<TxId>>,
}

fn detect_cycle(start_tx: TxId) -> Option<Vec<TxId>> {
    // DFS 检测环
}
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 死锁环检测 | ⏳ | 构造死锁场景 |
| victim 选择 | ⏳ | 最小等待时间 |
| abort 生效 | ⏳ | 事务回滚 |
| 无死锁 hang | ⏳ | 超时检测 |

**文件位置**: `crates/concurrency/src/deadlock.rs`
**测试文件**: `crates/concurrency/tests/deadlock_test.rs`

---

### P-01 查询缓存 (Query Cache)

```rust
struct CacheKey {
    sql_hash: u64,
    params_hash: u64,
    table_versions: Vec<u64>,
}

struct QueryCache {
    cache: DashMap<CacheKey, QueryResult>,
}
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 相同 SQL 命中缓存 | ✅ | 缓存命中测试 |
| 数据变更缓存失效 | ✅ | invalidate 测试 |
| 多表依赖正确 | ✅ | 多表查询测试 |

**文件位置**: `crates/executor/src/query_cache.rs`
**测试文件**: `crates/executor/tests/query_cache_test.rs`

---

### P-02 连接池 (Connection Pool)

```rust
struct ConnectionPool {
    max_size: usize,
    semaphore: Semaphore,
    connections: Mutex<Vec<Connection>>,
}
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 连接获取 | ⏳ | acquire 测试 |
| 连接释放 | ⏳ | release 测试 |
| 50+ 并发 | ⏳ | 压力测试 |
| 超时处理 | ⏳ | timeout 测试 |

**文件位置**: `crates/pool/src/connection_pool.rs`
**测试文件**: `crates/pool/tests/connection_pool_test.rs`

---

### P-03 TPC-H Benchmark

#### 数据生成

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| customer.csv | ⏳ | 1000 行 |
| orders.csv | ⏳ | 5000 行 |
| lineitem.csv | ⏳ | 10000 行 |
| 外键关系 | ⏳ | FK 验证 |

**文件位置**: `crates/bench/examples/tpch_data_gen.rs`

#### Q1 查询

```sql
SELECT l_returnflag, l_linestatus,
       SUM(l_quantity), SUM(l_extendedprice), ...
FROM lineitem
WHERE l_shipdate <= DATE '1998-09-02'
GROUP BY l_returnflag, l_linestatus
ORDER BY l_returnflag, l_linestatus;
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 执行无错误 | ⏳ | benchmark 测试 |
| 结果正确 | ⏳ | 对比 SQLite |
| 延迟 < 500ms | ⏳ | P99 测试 |

#### Q6 查询

```sql
SELECT SUM(l_extendedprice * l_discount) AS revenue
FROM lineitem
WHERE l_shipdate >= DATE '1994-01-01'
  AND l_shipdate < DATE '1995-01-01'
  AND l_discount BETWEEN 0.05 AND 0.07
  AND l_quantity < 24;
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 执行无错误 | ⏳ | benchmark 测试 |
| 结果正确 | ⏳ | 对比 SQLite |
| 延迟 < 200ms | ⏳ | P99 测试 |

**文件位置**: `benches/tpch_bench.rs`, `benches/sqlite_compare.rs`

---

### D-01/D-02 DATE/TIMESTAMP

```rust
struct Date {
    days_since_epoch: i32,
}

struct Timestamp {
    micros_since_epoch: i64,
}
```

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| DATE 解析 | ✅ | 解析测试 |
| DATE 范围计算 | ✅ | days_between 测试 |
| TIMESTAMP 解析 | ✅ | 解析测试 |
| TIMESTAMP 格式化 | ✅ | to_string 测试 |

**文件位置**: `crates/types/src/date.rs`, `crates/types/src/timestamp.rs`

---

## 三、PR 拆分

| PR | 内容 | 文件数 | 目标行数 | 依赖 |
|----|------|--------|----------|------|
| #633 | LockManager | 2 | ≤500 | 无 |
| #628 | Deadlock Detection | 2 | ≤300 | #633 |
| #627 | Query Cache | 2 | ≤400 | 无 |
| #630 | Connection Pool | 2 | ≤300 | 无 |
| #631 | TPC-H Data Gen | 3 | ≤300 | 无 |
| #631 | TPC-H Q1/Q6 + SQLite | 3 | ≤400 | #631 |
| #624/#634 | Date/Time Type | 4 | ≤400 | 无 |

---

## 四、CI 检查清单

每个 PR 必须通过：

- [ ] `cargo build --workspace`
- [ ] `cargo test --workspace`
- [ ] `cargo clippy --workspace -- -D warnings`
- [ ] `cargo fmt --all -- --check`
- [ ] 相关模块单元测试
- [ ] 覆盖率不下降

---

## 五、发布检查清单

v1.6.0 发布前：

- [ ] 所有 7 个 PR 合并
- [ ] 覆盖率 ≥ 75%
- [ ] TPC-H Q1/Q6 执行成功
- [ ] SQLite 结果对比通过
- [ ] Benchmark 报告生成
- [ ] 50 并发测试通过
- [ ] 死锁检测测试通过
- [ ] README 更新
- [ ] CHANGELOG 更新
- [ ] VERSION 改为 v1.6.0
- [ ] Git tag v1.6.0

---

*本文档由 yinglichina8848 维护*
