# v2.8.0 性能目标

> 版本: `v2.8.0`
> 日期: 2026-04-23
> 状态: Alpha (规划中)

---

## 一、性能目标总览

| 指标 | v2.7.0 | v2.8.0 目标 | 提升幅度 |
|------|---------|--------------|----------|
| TPC-H SF=1 Q1 | ~3s | < 2s | 1.5x |
| SIMD 加速比 | N/A | ≥ 2x | - |
| Hash Join 并行 | 单线程 | 2+ 线程 | ≥ 1.5x |
| 故障转移时间 | N/A | < 30s | - |
| QPS (简单查询) | ~500 | ≥ 1000 | 2x |
| 内存使用 | < 8GB | < 8GB | - |

---

## 二、TPC-H 基准测试

### 2.1 测试环境

| 配置 | 值 |
|------|-----|
| Scale Factor | SF=1 (1GB 数据) |
| 查询 | TPC-H 22 条查询 |
| 预热 | 3 分钟 |
| 迭代 | 3 次，取中位数 |

### 2.2 目标 (SF=1)

| 查询 | v2.7.0 | v2.8.0 目标 |
|------|---------|-------------|
| Q1 (价格统计) | ~3s | < 2s |
| Q2 (最小SuppCost) | ~2s | < 1.5s |
| Q3 (订单优先级) | ~2.5s | < 2s |
| Q4 (订单优先级统计) | ~2s | < 1.5s |
| Q5 (本地市场收入) | ~2s | < 1.5s |
| Q6 (收入预测) | ~1.5s | < 1s |
| Q7 (装运折扣) | ~2s | < 1.5s |
| Q8 (国家市场份额) | ~2.5s | < 2s |
| Q9 (产品类型) | ~3s | < 2.5s |
| Q10 (退款查询) | ~2s | < 1.5s |
| Q11 (库存价值) | ~1.5s | < 1s |
| Q12 (订单优先级) | ~2s | < 1.5s |
| Q13 (客户订单) | ~3s | < 2s |
| Q14 (促销效果) | ~1.5s | < 1s |
| Q15 (top Supplier) | ~2s | < 1.5s |
| Q16 (零件/供应检查) | ~2s | < 1.5s |
| Q17 (小订单收入) | ~3s | < 2s |
| Q18 (大订单查询) | ~4s | < 3s |
| Q19 (折扣收入) | ~2s | < 1.5s |
| Q20 (通知供应商) | ~2.5s | < 2s |
| Q21 (停滞供应商) | ~4s | < 3s |
| Q22 (全球销售查询) | ~2s | < 1.5s |
| **总计** | ~45s | < 30s |

### 2.3 测试命令

```bash
# 使用 go-tpc 执行 TPC-H
go-tpc run -H 127.0.0.1:3306 -D tpch -T 1 -t 3 -p password Q1

# 或使用 sqlpp 本地测试
sqlpp -d tpch -f queries/q1.sql
```

---

## 三、SIMD 向量化性能

### 3.1 加速目标

| 操作 | 加速比 | 说明 |
|------|--------|------|
| **向量化扫描** | ≥ 2x | SIMD vs 非 SIMD |
| **聚合计算** | ≥ 1.8x | SUM/AVG/COUNT |
| **字符串操作** | ≥ 1.5x | LIKE/UPPER/LOWER |

### 3.2 测试方法

```bash
# 1. 编译 SIMD 开启版本
cargo build --release --features simd

# 2. 运行 micro-benchmark
cargo bench --package sqlrustgo-executor --lib -- projection_benchmark

# 3. 对比结果
# SIMD 版本 vs 非 SIMD 版本
```

### 3.3 CPU 利用率目标

| 场景 | 目标 | 说明 |
|------|------|------|
| SIMD 向量化 | < 80% | 避免 CPU 100% |
| 多核并行 | < 90% | 每核 < 90% |

---

## 四、Hash Join 并行化

### 4.1 目标

| 指标 | 单线程 | 多线程目标 |
|------|--------|------------|
| **线程数** | 1 | ≥ 2 |
| **加速比** | 1x | ≥ 1.5x |
| **内存使用** | < 4GB | < 8GB |
| **CPU 利用率** | 100% (单核) | < 800% (8核) |

### 4.2 测试方法

```bash
# 1. 编译
cargo build --release

# 2. 运行 Hash Join 基准
cargo bench --package sqlrustgo-executor --lib -- hash_join_benchmark

# 3. 测试不同数据规模
- 小表 (1K rows) × 大表 (1M rows)
- 大表 (1M rows) × 大表 (1M rows)
```

### 4.3 预期结果

| 数据规模 | 单线程 | 2 线程 | 4 线程 | 8 线程 |
|----------|--------|--------|--------|--------|
| 1K × 1M | 100ms | 60ms | 40ms | 35ms |
| 1M × 1M | 2s | 1.3s | 0.9s | 0.7s |

---

## 五、分布式性能

### 5.1 负载均衡性能

| 指标 | 目标 | 说明 |
|------|------|------|
| **QPS (单节点)** | ~500 | 基准 |
| **QPS (3 节点集群)** | ≥ 1200 | 线性扩展 > 80% |
| **延迟 P99** | < 100ms | 单查询 |
| **并发连接** | ≥ 500 | 负载均衡器 |

### 5.2 故障转移性能

| 指标 | 目标 | 说明 |
|------|------|------|
| **故障检测** | < 5s | 主节点不可达 |
| **故障转移** | < 30s | 自动切换 |
| **写恢复** | < 5s | 新主节点可写 |
| **总恢复时间** | < 40s | 业务恢复 |

### 5.3 复制延迟

| 指标 | 目标 | 说明 |
|------|------|------|
| **单行 INSERT** | < 100ms | 端到端 |
| **批量 INSERT (100 rows)** | < 500ms | 端到端 |
| **事务提交** | < 200ms | 端到端 |

---

## 六、内存使用

### 6.1 内存目标

| 场景 | 内存上限 | 说明 |
|------|----------|------|
| **单查询** | < 512MB | 峰值 |
| **TPC-H Q1** | < 2GB | SF=1 |
| **Hash Join** | < 8GB | 并行查询 |
| **复制缓冲** | < 1GB | 主从复制 |

### 6.2 监控指标

```bash
# 使用 memory_profiler 或 Valgrind
cargo run --release --bin sqlrustgo &
valgrind --tool=massif ./target/release/sqlrustgo

# 或使用 /proc/[pid]/status
grep VmRSS /proc/$(pgrep sqlrustgo)/status
```

---

## 七、并发性能

### 7.1 连接池

| 指标 | 目标 | 说明 |
|------|------|------|
| **最大连接** | ≥ 500 | 客户端连接 |
| **连接获取** | < 10ms | 从池获取连接 |
| **空闲超时** | 300s | 空闲连接回收 |

### 7.2 并发查询

| 指标 | 目标 | 说明 |
|------|------|------|
| **简单 SELECT** | ≥ 1000 QPS | 单节点 |
| **复杂 JOIN** | ≥ 100 QPS | 多表 JOIN |
| **写入** | ≥ 500 QPS | INSERT/UPDATE |

### 7.3 压测命令

```bash
# 使用 wrk 进行 HTTP 压测
wrk -t4 -c100 -d30s http://localhost:8080/query?sql="SELECT%20*%20FROM%20t%20WHERE%20id=1"

# 使用 mysqlslap 进行 MySQL 压测
mysqlslap -h localhost -u root -p --concurrency=100 --iterations=10 --query="SELECT * FROM t WHERE id=1"
```

---

## 八、性能回归基线

### 8.1 性能基线记录

| 测试 | 基线值 | 阈值 | 回归判定 |
|------|--------|------|----------|
| TPC-H Q1 | 2s | 2.5s (+25%) | > 2.5s |
| TPC-H 总计 | 30s | 37.5s (+25%) | > 37.5s |
| Hash Join 1M×1M | 2s | 2.5s (+25%) | > 2.5s |
| 简单 SELECT QPS | 500 | 400 (-20%) | < 400 |
| 写入 QPS | 500 | 400 (-20%) | < 400 |

### 8.2 性能测试频率

| 里程碑 | 测试频率 | 说明 |
|--------|----------|------|
| Alpha | 每周 | 监控趋势 |
| Beta | 每 3 天 | 验证优化 |
| RC | 每次代码变更 | 确保无回归 |

---

## 九、性能优化建议

### 9.1 Phase C 优化优先级

| 优先级 | 优化项 | 预期收益 | 难度 |
|--------|--------|----------|------|
| P0 | SIMD 向量化 | 2x | 高 |
| P0 | Hash Join 并行化 | 1.5x | 中 |
| P1 | 查询计划器优化 | 1.3x | 高 |
| P1 | 索引优化 | 1.5x | 中 |

### 9.2 优化技巧

```rust
// SIMD 向量化示例
#[cfg(target_arch = "x86_64")]
use std::arch::x86_64::*;

fn simd_sum(values: &[i64]) -> i64 {
    // 使用 SIMD 指令并行求和
}

// Hash Join 并行化示例
fn parallel_hash_join(left: &Table, right: &Table, threads: usize) -> Result<Table> {
    // 使用 Rayon 并行处理
    left.par_iter()
        .map(|(k, v)| build_hash_table(k, v))
        .collect()
}
```

---

*本文档由 SQLRustGo Team 维护*
*最后更新: 2026-04-23*