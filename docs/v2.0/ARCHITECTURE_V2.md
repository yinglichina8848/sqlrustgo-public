# SQLRustGo 2.0 架构与演进设计

> **版本**: 2.0  
> **状态**: 架构稳定  
> **目标**: 为未来 3.0 分布式数据库内核安全演进做准备  
> **更新日期**: 2026-03-05

---

## 目录

1. [系统架构](#一系统架构)
2. [风险矩阵](#二风险矩阵)
3. [3.0 分布式接口预留层设计](#三30-分布式接口预留层设计)
4. [演进路径](#四演进路径)
5. [总结](#五总结)

---

## 一、系统架构

### 1.1 系统总体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          SQLRustGo 2.0 系统架构                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          Client Layer                                │   │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │   │
│   │  │    CLI      │  │   MySQL     │  │    REST     │                  │   │
│   │  │   Client    │  │   Client    │  │    API      │                  │   │
│   │  └─────────────┘  └─────────────┘  └─────────────┘                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                         │
│                                    ▼                                         │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        Protocol Layer                                │   │
│   │  ┌─────────────────────────────────────────────────────────────┐    │   │
│   │  │                    MySQL Protocol Handler                    │    │   │
│   │  └─────────────────────────────────────────────────────────────┘    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                         │
│                                    ▼                                         │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         Query Layer                                  │   │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │   │
│   │  │   Parser    │──│  Analyzer   │──│  Optimizer  │                  │   │
│   │  │   (AST)     │  │  (Logical)  │  │  (Rules)    │                  │   │
│   │  └─────────────┘  └─────────────┘  └─────────────┘                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                         │
│                                    ▼                                         │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                       Execution Layer                                │   │
│   │  ┌─────────────────────────────────────────────────────────────┐    │   │
│   │  │                   Volcano Executor Model                     │    │   │
│   │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │    │   │
│   │  │  │  Scan   │ │ Filter  │ │  Join   │ │  Agg    │           │    │   │
│   │  │  │Executor │ │Executor │ │Executor │ │Executor │           │    │   │
│   │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │    │   │
│   │  └─────────────────────────────────────────────────────────────┘    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                         │
│                                    ▼                                         │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        Storage Layer                                 │   │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │   │
│   │  │ BufferPool  │  │  B+ Tree    │  │   WAL       │                  │   │
│   │  │  (Cache)    │  │  (Index)    │  │  (Log)      │                  │   │
│   │  └─────────────┘  └─────────────┘  └─────────────┘                  │   │
│   │  ┌─────────────────────────────────────────────────────────────┐    │   │
│   │  │                    Storage Engine Trait                      │    │   │
│   │  └─────────────────────────────────────────────────────────────┘    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 执行引擎内部结构（Volcano 模型）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Volcano 执行模型                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   Executor Trait (统一接口)                                                  │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  pub trait Executor: Send {                                          │   │
│   │      fn open(&mut self, ctx: &dyn ExecutionContext) -> Result<()>;  │   │
│   │      fn next(&mut self, ctx: &dyn ExecutionContext)                 │   │
│   │          -> Result<Option<RecordBatch>>;                            │   │
│   │      fn close(&mut self, ctx: &dyn ExecutionContext) -> Result<()>; │   │
│   │  }                                                                   │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   执行流程:                                                                  │
│                                                                              │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐            │
│   │  Scan    │───►│ Filter   │───►│  Join    │───►│ Project  │            │
│   │ Executor │    │ Executor │    │ Executor │    │ Executor │            │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘            │
│        │               │               │               │                   │
│        ▼               ▼               ▼               ▼                   │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐            │
│   │  open()  │    │  open()  │    │  open()  │    │  open()  │            │
│   │  next()  │◄───│  next()  │◄───│  next()  │◄───│  next()  │            │
│   │  close() │    │  close() │    │  close() │    │  close() │            │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘            │
│                                                                              │
│   说明:                                                                      │
│   • 每个物理算子实现统一的 Executor trait                                    │
│   • 支持拉取式数据流                                                        │
│   • 支持未来向量化升级                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 3.0 预留分布式结构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          3.0 分布式架构预览                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                       Coordinator Node                               │   │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │   │
│   │  │   Parser    │  │  Optimizer  │  │  Scheduler  │                  │   │
│   │  └─────────────┘  └─────────────┘  └─────────────┘                  │   │
│   │                         │                                            │   │
│   │                         ▼                                            │   │
│   │              ┌─────────────────────┐                                 │   │
│   │              │   Plan Fragmenter   │                                 │   │
│   │              └─────────────────────┘                                 │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                         │
│                    ┌───────────────┼───────────────┐                        │
│                    ▼               ▼               ▼                        │
│   ┌─────────────────────┐ ┌─────────────────────┐ ┌─────────────────────┐  │
│   │    Worker Node 1    │ │    Worker Node 2    │ │    Worker Node N    │  │
│   │  ┌───────────────┐  │ │  ┌───────────────┐  │ │  ┌───────────────┐  │  │
│   │  │ LocalExecutor │  │ │  │ LocalExecutor │  │ │  │ LocalExecutor │  │  │
│   │  └───────────────┘  │ │  └───────────────┘  │ │  └───────────────┘  │  │
│   │  ┌───────────────┐  │ │  ┌───────────────┐  │ │  ┌───────────────┐  │  │
│   │  │ Storage Engine│  │ │  │ Storage Engine│  │ │  │ Storage Engine│  │  │
│   │  └───────────────┘  │ │  └───────────────┘  │ │  └───────────────┘  │  │
│   └─────────────────────┘ └─────────────────────┘ └─────────────────────┘  │
│                                                                              │
│   数据交换层:                                                                │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                      DataExchange Interface                          │   │
│   │  • send_batch(target: NodeId, batch: RecordBatch)                   │   │
│   │  • receive_batch(source: NodeId) -> RecordBatch                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、风险矩阵

### 2.1 高风险（必须控制）

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| Trait 设计不稳定 | 中 | 高 | Phase0 阶段冻结核心 Trait |
| HashJoin 内存爆炸 | 中 | 高 | 提前设计 spill 接口 |
| 协议不可扩展 | 中 | 高 | 协议中增加版本字段 |
| 并发死锁 | 低 | 高 | 避免共享可变状态 |

### 2.2 中风险

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| 优化器规则冲突 | 中 | 中 | 控制规则执行顺序 |
| 性能低于预期 | 中 | 中 | 提前加入 Benchmark 测试 |
| 错误模型混乱 | 中 | 中 | 统一 Error 枚举 |

### 2.3 低风险

| 风险 | 说明 |
|------|------|
| CLI 功能不丰富 | 非核心功能，可后期迭代 |
| 日志系统基础 | 可在未来增强 |

### 2.4 核心设计风险

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          核心设计风险                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   只要这三点稳定，2.0 就安全：                                                │
│                                                                              │
│   1. Trait 设计                                                              │
│      └── Executor trait 接口稳定                                            │
│      └── Storage trait 接口稳定                                             │
│      └── ExecutionContext trait 接口稳定                                    │
│                                                                              │
│   2. 内存管理模型                                                            │
│      └── RecordBatch 内存布局稳定                                           │
│      └── BufferPool 接口稳定                                                │
│                                                                              │
│   3. 网络协议可扩展性                                                        │
│      └── MySQL 协议版本字段                                                 │
│      └── 扩展字段预留                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、3.0 分布式接口预留层设计

### 3.1 设计目标

> **2.0 单机 → 3.0 可安全升级为分布式执行**

### 3.2 参考设计理念

| 项目 | 参考点 |
|------|--------|
| TiDB | 分布式 SQL 执行 |
| Cockroach Labs | 分布式事务 |
| Apache Spark | Plan Fragment |

### 3.3 ExecutionContext 设计

#### NodeId 定义

```rust
#[derive(Clone, Debug, PartialEq, Eq, Hash)]
pub struct NodeId(pub u64);
```

#### ExecutionMode

```rust
#[derive(Clone, Debug)]
pub enum ExecutionMode {
    Local,
    Distributed,
}
```

#### ExecutionContext Trait

```rust
pub trait ExecutionContext: Send + Sync {
    fn node_id(&self) -> NodeId;
    
    fn mode(&self) -> ExecutionMode;
    
    fn is_distributed(&self) -> bool {
        matches!(self.mode(), ExecutionMode::Distributed)
    }
    
    fn is_coordinator(&self) -> bool {
        self.is_distributed()
    }
}
```

#### LocalExecutionContext (2.0 实现)

```rust
pub struct LocalExecutionContext {
    node_id: NodeId,
}

impl LocalExecutionContext {
    pub fn new(node_id: NodeId) -> Self {
        Self { node_id }
    }
}

impl ExecutionContext for LocalExecutionContext {
    fn node_id(&self) -> NodeId {
        self.node_id.clone()
    }
    
    fn mode(&self) -> ExecutionMode {
        ExecutionMode::Local
    }
}
```

### 3.4 Executor Trait（兼容 3.0）

```rust
pub trait Executor: Send {
    fn open(&mut self, ctx: &dyn ExecutionContext) -> Result<()>;
    
    fn next(&mut self, ctx: &dyn ExecutionContext) 
        -> Result<Option<RecordBatch>>;
    
    fn close(&mut self, ctx: &dyn ExecutionContext) -> Result<()>;
}
```

**设计要点**:
- 所有算子必须接收 ExecutionContext
- 支持未来分布式、远程数据拉取、统计信息收集

### 3.5 Plan Fragment

```rust
#[derive(Clone, Debug)]
pub struct PlanFragment {
    pub fragment_id: u32,
    pub root: PhysicalPlanNode,
}
```

**流程示意**:

```
协调节点
   │
   ▼
拆分物理计划
   │
   ▼
发送 PlanFragment 给工作节点
   │
   ▼
执行
   │
   ▼
合并结果
```

### 3.6 DataExchange 接口

```rust
pub trait DataExchange: Send + Sync {
    fn send_batch(&self, target: NodeId, batch: RecordBatch) -> Result<()>;
    
    fn receive_batch(&self, source: NodeId) -> Result<Option<RecordBatch>>;
}
```

**2.0 默认实现**:

```rust
pub struct NoopExchange;

impl DataExchange for NoopExchange {
    fn send_batch(&self, _target: NodeId, _batch: RecordBatch) -> Result<()> {
        Ok(())
    }
    
    fn receive_batch(&self, _source: NodeId) -> Result<Option<RecordBatch>> {
        Ok(None)
    }
}
```

### 3.7 Storage Trait（可插拔）

```rust
pub trait StorageEngine: Send + Sync {
    fn scan(
        &self, 
        table: &str,
    ) -> Result<Box<dyn Iterator<Item = RecordBatch>>>;
}
```

**未来实现**:

| 实现 | 说明 |
|------|------|
| MemoryStorage | 内存存储 |
| RocksDBStorage | RocksDB 存储 |
| RemoteStorage | 远程存储 |
| DistributedKVStorage | 分布式 KV 存储 |

---

## 四、演进路径

### 4.1 版本演进图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          版本演进路径                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   v1.x (当前)                                                               │
│   ├── 单机执行                                                              │
│   ├── 基础 SQL 支持                                                         │
│   ├── B+ Tree 索引                                                          │
│   └── WAL 事务                                                              │
│        │                                                                     │
│        ▼                                                                     │
│   v2.0 (规划中)                                                              │
│   ├── Volcano 执行模型                                                      │
│   ├── 规则优化器                                                            │
│   ├── Client/Server 架构                                                    │
│   ├── 可插拔存储基础                                                        │
│   └── 分布式接口预留                                                        │
│        │                                                                     │
│        ▼                                                                     │
│   v3.0 (未来)                                                               │
│   ├── Fragment 分布式执行                                                   │
│   ├── 数据交换算子                                                          │
│   ├── 多节点协调                                                            │
│   └── 分布式存储                                                            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 接口稳定性保证

| 版本 | 接口 | 状态 |
|------|------|------|
| 2.0 | Executor trait | ✅ 冻结 |
| 2.0 | StorageEngine trait | ✅ 冻结 |
| 2.0 | ExecutionContext trait | ✅ 冻结 |
| 2.0 | RecordBatch | ✅ 冻结 |
| 3.0 | DataExchange trait | 📝 新增 |
| 3.0 | PlanFragment | 📝 新增 |

---

## 五、总结

### 5.1 SQLRustGo 2.0 特性

| 特性 | 说明 |
|------|------|
| 单机执行 | 本地执行引擎 |
| Volcano 执行模型 | 拉取式数据流 |
| 规则优化器 | 基于规则的查询优化 |
| Client/Server 架构 | MySQL 协议支持 |
| 可插拔存储基础 | Storage trait 抽象 |

### 5.2 安全演进到 3.0

| 演进点 | 说明 |
|--------|------|
| Fragment 分布式执行 | PlanFragment 拆分 |
| 数据交换算子 | DataExchange 接口 |
| 多节点协调 | ExecutionContext 扩展 |
| 存储接口抽象 | StorageEngine 实现 |

### 5.3 核心原则

> **2.0 不会在 3.0 被推翻，只会扩展。**

---

## 变更历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-03-05 | 初始版本 |
