# AI 协作开发教程

> 基于 SQLRustGo 项目实践，教你如何使用 AI-IDE、AI-CLI 和 OpenClaw 进行高效的多 Agent 协作开发

---

## 目录

1. [概述：AI 协作开发的新范式](#1-概述ai-协作开发的新范式)
2. [工具链概览](#2-工具链概览)
3. [AI-IDE：Trae/Claude Code 实战](#3-ai-idetraeclaude-code-实战)
4. [AI-CLI：命令行 AI 工具](#4-ai-cliaicli-命令行-ai-工具)
5. [OpenClaw：多平台 AI 管理](#5-openclaw多平台-ai-管理)
6. [GitHub 多账号配置](#6-github-多账号配置)
7. [多 Agent 协作模式](#7-多-agent-协作模式)
8. [实战案例：SQLRustGo 项目](#8-实战案例sqlrustgo-项目)
9. [最佳实践与注意事项](#9-最佳实践与注意事项)

---

## 1. 概述：AI 协作开发的新范式

### 1.1 传统开发 vs AI 协作开发

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        传统开发模式                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   开发者 ──→ 编写代码 ──→ 调试 ──→ 提交 ──→ 审查 ──→ 合并                    │
│                                                                              │
│   特点：单线程、人工驱动、效率受限                                            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        AI 协作开发模式                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   开发者 ──→ 任务分配 ──→ AI Agent 并行开发 ──→ 自动审查 ──→ 人工确认        │
│      │                    │                    │                             │
│      │                    ├── Agent A: 解析器   │                             │
│      │                    ├── Agent B: 执行器   │                             │
│      │                    └── Agent C: 网络层   │                             │
│      │                                                                       │
│   特点：多线程、AI 驱动、效率倍增                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心理念

| 理念 | 说明 |
|:-----|:-----|
| **人机协同** | 人类负责决策和审核，AI 负责执行和实现 |
| **并行开发** | 多个 AI Agent 同时处理不同模块 |
| **流程规范** | 严格的分支管理和代码审查机制 |
| **持续验证** | 自动化测试和 CI/CD 保证质量 |

---

## 2. 工具链概览

### 2.1 工具矩阵

| 工具类型 | 工具名称 | 核心功能 | 适用场景 |
|:---------|:---------|:---------|:---------|
| **AI-IDE** | Trae / Claude Code | 代码编写、重构、调试 | 主要开发工作 |
| **AI-CLI** | Claude CLI / Codex | 命令行 AI 交互 | 自动化脚本、CI/CD |
| **多平台管理** | OpenClaw | 统一管理多个 AI 平台 | 多模型切换、成本优化 |
| **代理服务** | LiteLLM | API 代理和负载均衡 | 模型切换、用量统计 |
| **协作平台** | GitHub | 代码托管、PR 管理 | 版本控制、团队协作 |

### 2.2 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AI 协作开发架构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                   │
│   │   Trae IDE  │     │  Claude CLI │     │   OpenClaw  │                   │
│   │  (AI-IDE)   │     │  (AI-CLI)   │     │ (多平台管理) │                   │
│   └──────┬──────┘     └──────┬──────┘     └──────┬──────┘                   │
│          │                   │                   │                          │
│          └───────────────────┼───────────────────┘                          │
│                              │                                              │
│                              ▼                                              │
│                    ┌─────────────────┐                                      │
│                    │    LiteLLM      │                                      │
│                    │  (API 代理)      │                                      │
│                    └────────┬────────┘                                      │
│                             │                                               │
│              ┌──────────────┼──────────────┐                                │
│              │              │              │                                │
│              ▼              ▼              ▼                                │
│        ┌─────────┐   ┌─────────┐   ┌─────────┐                             │
│        │ Claude  │   │ GPT-4   │   │DeepSeek │                             │
│        └─────────┘   └─────────┘   └─────────┘                             │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         GitHub                                       │   │
│   │   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐            │   │
│   │   │  alpha  │   │  beta   │   │ release │   │baseline │            │   │
│   │   └─────────┘   └─────────┘   └─────────┘   └─────────┘            │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. AI-IDE：Trae/Claude Code 实战

### 3.1 Trae IDE 简介

Trae 是一款 AI 原生的集成开发环境，内置了强大的 AI 助手能力。

#### 核心功能

| 功能 | 说明 | 快捷键/命令 |
|:-----|:-----|:------------|
| **代码补全** | AI 驱动的智能补全 | 自动触发 |
| **代码解释** | 解释选中的代码 | 选中代码后询问 |
| **重构建议** | 提供重构建议 | "帮我重构这段代码" |
| **Bug 修复** | 自动诊断和修复 | "这个测试失败了，帮我修复" |
| **文档生成** | 自动生成文档注释 | "为这个函数生成文档" |

### 3.2 Claude Code 技能系统

Claude Code 提供了专业技能系统，可以针对特定任务调用专业能力：

```bash
# 可用技能列表
superpowers:brainstorming      # 功能设计讨论
superpowers:tdd-workflow       # TDD 开发流程
superpowers:code-review        # 代码审查
superpowers:verification       # 完成前验证
superpowers:using-git-worktrees # 隔离开发环境
```

#### 使用示例

```
用户: 使用 brainstorming 技能帮我设计 JOIN 查询的实现

AI: 好的，让我使用 brainstorming 技能来分析 JOIN 查询的实现方案...

[AI 会系统地分析需求、设计方案、评估风险]
```

### 3.3 实战：使用 Trae 开发新功能

#### 场景：实现 ORDER BY 功能

**步骤 1：描述需求**

```
用户: 我需要实现 SQL 的 ORDER BY 功能，支持 ASC 和 DESC 排序
```

**步骤 2：AI 分析并设计**

```
AI: 让我分析一下 ORDER BY 的实现方案：

1. 解析器扩展
   - 添加 ORDER BY 子句的语法解析
   - 支持 ASC/DESC 关键字

2. 执行器修改
   - 在查询计划中添加 Sort 节点
   - 实现排序算法

3. 测试用例
   - 单列排序
   - 多列排序
   - NULL 值处理

是否开始实现？
```

**步骤 3：确认并实现**

```
用户: 好的，开始实现

AI: [开始编写代码、测试、文档...]
```

### 3.4 最佳实践

| 实践 | 说明 |
|:-----|:-----|
| **明确需求** | 提供清晰的上下文和期望结果 |
| **分步确认** | 大任务拆分为小步骤，逐步确认 |
| **验证结果** | 让 AI 运行测试，确保代码正确 |
| **保留记录** | 重要对话保存为文档，便于回溯 |

---

## 4. AI-CLI：命令行 AI 工具

### 4.1 Claude CLI 安装与配置

```bash
# 安装 Claude CLI
npm install -g @anthropic-ai/claude-code

# 配置 API Key
export ANTHROPIC_API_KEY=your-api-key

# 验证安装
claude --version
```

### 4.2 常用命令

```bash
# 启动交互式会话
claude

# 执行单次任务
claude "帮我分析这个文件的代码质量: src/parser.rs"

# 使用特定模型
claude --model claude-3-opus "优化这个函数的性能"

# 输出到文件
claude "生成 README 文档" > README.md
```

### 4.3 AI CLI 身份配置

在多 Agent 协作中，每个 CLI 需要有自己的身份标识：

```bash
# 创建身份配置脚本
cat > ~/bin/ai-identity.sh << 'EOF'
#!/bin/bash

case "$1" in
    "agent-a")
        export AI_NAME="Agent-A"
        export AI_ROLE="Parser Developer"
        export AI_SIGN="🤖 Agent-A"
        ;;
    "agent-b")
        export AI_NAME="Agent-B"
        export AI_ROLE="Executor Developer"
        export AI_SIGN="🤖 Agent-B"
        ;;
    "agent-c")
        export AI_NAME="Agent-C"
        export AI_ROLE="Network Developer"
        export AI_SIGN="🤖 Agent-C"
        ;;
esac
EOF

chmod +x ~/bin/ai-identity.sh
```

### 4.4 自动化脚本示例

```bash
#!/bin/bash
# auto-review.sh - 自动代码审查脚本

# 设置身份
source ~/bin/ai-identity.sh agent-a

# 获取变更文件
CHANGED_FILES=$(git diff --name-only origin/alpha...HEAD)

# 对每个文件进行审查
for file in $CHANGED_FILES; do
    echo "审查文件: $file"
    claude "请审查这个文件的变更，检查：
    1. 代码风格
    2. 潜在 Bug
    3. 性能问题
    4. 安全风险
    
    文件: $file" >> review-report.md
done

echo "审查报告已生成: review-report.md"
```

---

## 5. OpenClaw：多平台 AI 管理

### 5.1 OpenClaw 简介

OpenClaw 是一个开源的多平台 AI 管理系统，支持：

- 统一管理多个 AI 平台账号
- 自动切换最优模型
- 成本统计和优化
- 多 Agent 协作支持

### 5.2 安装与配置

```bash
# 克隆项目
git clone https://github.com/your-org/openclaw.git
cd openclaw

# 安装依赖
pip install -r requirements.txt

# 配置文件
cat > config.yaml << 'EOF'
platforms:
  - name: claude
    api_key: ${ANTHROPIC_API_KEY}
    models:
      - claude-3-opus
      - claude-3-sonnet
      
  - name: openai
    api_key: ${OPENAI_API_KEY}
    models:
      - gpt-4
      - gpt-3.5-turbo
      
  - name: deepseek
    api_key: ${DEEPSEEK_API_KEY}
    models:
      - deepseek-chat
      - deepseek-coder

routing:
  default: claude-3-sonnet
  code_generation: deepseek-coder
  code_review: claude-3-opus
  documentation: gpt-4
EOF
```

### 5.3 使用方式

```bash
# 启动 OpenClaw 服务
python main.py

# 在另一个终端使用
curl -X POST http://localhost:8080/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "帮我实现一个 B+ Tree 索引",
    "task_type": "code_generation"
  }'
```

### 5.4 LiteLLM 集成

LiteLLM 提供统一的 API 接口，支持多种模型：

```python
# litellm_config.py
from litellm import completion

# 统一调用方式，自动路由到不同模型
response = completion(
    model="claude-3-sonnet",  # 或 gpt-4, deepseek-chat
    messages=[
        {"role": "user", "content": "实现一个排序算法"}
    ]
)
```

---

## 6. GitHub 多账号配置

### 6.1 为什么需要多账号？

在 AI 协作开发中，多个 Agent 需要使用不同的 GitHub 身份：

- **身份隔离**：区分人类提交和 AI 提交
- **权限控制**：不同身份有不同的分支权限
- **审计追踪**：清晰记录每个变更的来源

### 6.2 核心架构：工作目录隔离

```
workspace/
   sona/           # sonaopenheart 身份（人类）
       sqlrustgo
       sqlrg-testing
   ai/             # ai-yingli-dev 身份（AI Agent）
       sqlrustgo
       sqlrg-testing
```

### 6.3 SSH 配置

```bash
# 生成 SSH 密钥对
ssh-keygen -t ed25519 -C "sona@example.com" -f ~/.ssh/id_ed25519_sona
ssh-keygen -t ed25519 -C "ai@example.com" -f ~/.ssh/id_ed25519_ai

# 配置 SSH
cat >> ~/.ssh/config << 'EOF'
Host github-sona
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_sona

Host github-ai
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_ai
EOF
```

### 6.4 目录设置

```bash
# sona 工作目录设置
cd ~/workspace/sona/sqlrustgo
git remote set-url origin git@github-sona:org/sqlrustgo.git
git config user.name "sonaopenheart"
git config user.email "sona@example.com"
export GH_CONFIG_DIR=~/workspace/sona/.gh

# ai 工作目录设置
cd ~/workspace/ai/sqlrustgo
git remote set-url origin git@github-ai:org/sqlrustgo.git
git config user.name "ai-yingli-dev"
git config user.email "ai@example.com"
export GH_CONFIG_DIR=~/workspace/ai/.gh
```

### 6.5 切换脚本

```bash
#!/bin/bash
# switch-identity.sh - 切换 GitHub 身份

case "$1" in
    "sona")
        cd ~/workspace/sona/sqlrustgo
        export GH_CONFIG_DIR=~/workspace/sona/.gh
        echo "切换到 sonaopenheart 身份"
        ;;
    "ai")
        cd ~/workspace/ai/sqlrustgo
        export GH_CONFIG_DIR=~/workspace/ai/.gh
        echo "切换到 ai-yingli-dev 身份"
        ;;
    *)
        echo "用法: source switch-identity.sh [sona|ai]"
        ;;
esac
```

---

## 7. 多 Agent 协作模式

### 7.1 协作架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        多 Agent 协作架构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         人类协调者                                   │   │
│   │                    (任务分配、最终审核)                               │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                    ┌───────────────┼───────────────┐                        │
│                    │               │               │                        │
│                    ▼               ▼               ▼                        │
│              ┌─────────┐     ┌─────────┐     ┌─────────┐                    │
│              │ Agent A │     │ Agent B │     │ Agent C │                    │
│              │ 解析器   │     │ 执行器   │     │ 网络层   │                    │
│              └────┬────┘     └────┬────┘     └────┬────┘                    │
│                   │               │               │                         │
│                   └───────────────┼───────────────┘                         │
│                                   │                                         │
│                                   ▼                                         │
│                         ┌─────────────────┐                                 │
│                         │  协作平台 (飞书)  │                                 │
│                         │   消息同步协调   │                                 │
│                         └─────────────────┘                                 │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 分支策略

```
alpha
  ├── feature/parser-refactor      # Agent A: 解析器重构
  ├── feature/executor-optimization # Agent B: 执行器优化
  ├── feature/network-layer        # Agent C: 网络层改进
  └── feature/test-coverage        # Agent D: 测试覆盖率提升
```

### 7.3 Agent 权限矩阵

| 分支 | 人类 | AI Agent |
|:-----|:-----|:---------|
| **alpha** | ✅ 完全权限 | ✅ 可开发 |
| **beta** | ✅ 审核权限 | ⚠️ 仅修 Bug |
| **release** | ✅ 审核权限 | ❌ 禁止写入 |
| **baseline** | ⚠️ 只读+PR | ❌ 禁止 |

### 7.4 协作流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Agent 协作流程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 任务分配                                                                │
│      人类协调者在协作平台发布任务，@ 对应的 Agent                             │
│                                                                              │
│   2. Agent 接收任务                                                          │
│      Agent 确认任务，创建 feature 分支                                        │
│                                                                              │
│   3. 并行开发                                                                │
│      各 Agent 独立开发，定期汇报进度                                          │
│                                                                              │
│   4. 提交 PR                                                                 │
│      Agent 完成后提交 PR，关联 Issue                                          │
│                                                                              │
│   5. 代码审查                                                                │
│      人类或其他 Agent 审查代码                                                │
│                                                                              │
│   6. 合并                                                                    │
│      审查通过后合并到 alpha 分支                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.5 协作平台使用规范

使用飞书等协作平台时，遵循以下规范：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        协作平台消息规范                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ✅ 正确做法：                                                              │
│                                                                              │
│   @Agent-A 请你负责解析器重构任务                                            │
│   @Agent-B 执行器优化任务分配给你                                            │
│                                                                              │
│   ❌ 错误做法：                                                              │
│                                                                              │
│   今天天气不错（没有 @ 任何人，Agent 不应回复）                               │
│   大家加油！（群发消息，不是任务要求）                                        │
│                                                                              │
│   规则：                                                                     │
│   - 消息是说给谁的，就 @ 谁                                                  │
│   - 没有 @ 的，不是任务/要求，可能是闲聊                                     │
│   - 不是对自己说的，不需要回复                                               │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 实战案例：SQLRustGo 项目

### 8.1 项目背景

SQLRustGo 是一个用 Rust 实现的 SQL 数据库内核，采用 AI 协作开发模式。

### 8.2 开发历程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SQLRustGo 开发历程                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   2026-02-13    项目启动                                                     │
│       │        Claude brainstorming 分析 sqlcc 项目                          │
│       │        确定技术栈：Rust 全新实现                                      │
│       │                                                                       │
│   2026-02-14    核心功能开发                                                 │
│       │        多 Agent 并行开发                                              │
│       │        - Agent A: 解析器                                              │
│       │        - Agent B: 执行器                                              │
│       │        - Agent C: 网络层                                              │
│       │                                                                       │
│   2026-02-16    测试覆盖率提升                                               │
│       │        从 60% 提升到 80%+                                             │
│       │        使用 AI 自动生成测试用例                                       │
│       │                                                                       │
│   2026-02-18    Alpha 版本发布                                               │
│       │        v1.0.0-alpha.0 标签创建                                        │
│       │        文档完善和治理规范建立                                         │
│       │                                                                       │
│   2026-02-19    Beta 版本准备                                                │
│                分支策略优化                                                   │
│                版本推进流程建立                                               │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.3 分支结构

```
main
  │
  ├── baseline (基线分支，只读)
  │
  ├── release (发布分支)
  │
  ├── beta (测试分支)
  │
  └── alpha (开发分支)
       │
       ├── feature/parser-refactor
       ├── feature/executor-optimization
       ├── feature/network-layer
       └── feature/test-coverage
```

### 8.4 典型协作场景

#### 场景 1：并行功能开发

```
协调者: @Agent-A @Agent-B @Agent-C 
        今天的目标是完成以下任务：
        - Agent-A: 实现 ORDER BY 功能
        - Agent-B: 优化 JOIN 执行性能
        - Agent-C: 添加连接池支持

Agent-A: 收到！开始实现 ORDER BY 功能 🤖
         已创建分支: feature/order-by

Agent-B: 收到！开始优化 JOIN 执行 🤖
         已创建分支: feature/join-optimization

Agent-C: 收到！开始添加连接池 🤖
         已创建分支: feature/connection-pool
```

#### 场景 2：代码审查

```
Agent-A: ORDER BY 功能已完成，提交 PR #42

协调者: @Agent-B 请审查 PR #42

Agent-B: 审查完成，发现 2 个问题：
         1. NULL 值排序处理不正确
         2. 缺少多列排序测试
         建议修改后重新提交

Agent-A: 收到，正在修复...

Agent-A: 已修复，请重新审查

Agent-B: 审查通过 ✅

协调者: 合并 PR #42
```

### 8.5 成果统计

| 指标 | 数值 |
|:-----|:-----|
| 总提交数 | 50+ |
| PR 数量 | 27+ |
| 测试覆盖率 | 80%+ |
| 文档页数 | 100+ |
| 开发周期 | 1 周 |

---

## 9. 最佳实践与注意事项

### 9.1 最佳实践

#### 实践 1：明确的任务描述

```
✅ 好的任务描述：

实现 ORDER BY 功能，要求：
1. 支持 ASC/DESC 关键字
2. 支持多列排序
3. 正确处理 NULL 值
4. 添加单元测试
5. 更新相关文档

❌ 不好的任务描述：

帮我做排序功能
```

#### 实践 2：分步验证

```
1. 先实现核心功能
2. 运行测试验证
3. 添加边界情况处理
4. 再次测试验证
5. 完善文档
```

#### 实践 3：保留记录

```
重要对话和决策应保存为文档：
- docs/v1.0/Claude对话记录.md
- docs/v1.0/飞书-龙虾群聊记录.md
- docs/v1.0/小龙虾的群聊记录.md
```

### 9.2 注意事项

#### 注意 1：权限控制

```
AI Agent 禁止：
❌ 直接推 baseline 分支
❌ 直接推 release 分支
❌ 跳过 PR 直接合并
❌ 自行 approve 自己的 PR
```

#### 注意 2：冲突解决

```
冲突解决规则：
1. 不允许自动 force push
2. 必须 rebase 到最新 alpha
3. 冲突必须人工 review
4. 冲突解决后重新测试
```

#### 注意 3：消息风暴

```
避免消息风暴：
1. 消息是说给谁的，就 @ 谁
2. 没说 @ 谁，就不是任务/要求
3. 不是对自己说的，不回复
4. 汇报工作时不要 @ 所有人
```

### 9.3 常见问题

#### Q1: Agent 之间如何协调工作？

```
A: 通过协作平台（如飞书）进行消息同步。
   人类协调者负责任务分配和冲突解决。
```

#### Q2: 如何保证代码质量？

```
A: 1. 每个 PR 必须通过 CI 测试
   2. 至少 1 个人工 review
   3. 遵循代码规范和最佳实践
```

#### Q3: 多个 Agent 修改同一文件怎么办？

```
A: 1. 尽量避免，通过任务分配隔离
   2. 发生冲突时，人工协调解决
   3. 记录冲突解决原因
```

---

## 附录

### A. 相关文档

| 文档 | 路径 |
|:-----|:-----|
| AI Agent 协作治理规范 | [AI_AGENT_COLLAB_GOVERNANCE.md](../AI_AGENT_COLLAB_GOVERNANCE.md) |
| AI 协作开发指南 | [2026-02-16-ai-collaboration-guide.md](草稿计划/2026-02-16-ai-collaboration-guide.md) |
| Claude 对话记录 | [Claude对话记录.md](Claude对话记录.md) |
| 飞书群聊记录 | [飞书-龙虾群聊记录.md](飞书-龙虾群聊记录.md) |
| 小龙虾群聊记录 | [小龙虾的群聊记录.md](小龙虾的群聊记录.md) |

### B. 工具链接

| 工具 | 链接 |
|:-----|:-----|
| Trae IDE | https://trae.ai |
| Claude | https://claude.ai |
| LiteLLM | https://github.com/BerriAI/litellm |
| GitHub CLI | https://cli.github.com |

### C. 版本历史

| 版本 | 日期 | 说明 |
|:-----|:-----|:-----|
| v1.0 | 2026-02-19 | 初始版本 |

---

> 本教程基于 SQLRustGo 项目实践经验编写，希望对你有所帮助！
