# AI Agent 协作治理规范

> 版本：v1.0
> 日期：2026-02-18
> 适用于：Codex / Claude Code / OpenCode / TRAE 等多模型并行协作

---

## 1. 分支与 Agent 权限隔离

| 分支 | 人类 | AI Agent |
|:-----|:-----|:---------|
| **alpha** | ✅ 完全权限 | ✅ 可开发 |
| **beta** | ✅ 审核权限 | ⚠️ 仅修 Bug |
| **release** | ✅ 审核权限 | ❌ 禁止写入 |
| **baseline** | ⚠️ 只读+PR | ❌ 禁止 |

---

## 2. Agent 提交流程

### AI 必须：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AI Agent 提交流程                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 创建 feature 分支                                                       │
│      git checkout alpha                                                      │
│      git checkout -b feature/xxx                                            │
│                                                                              │
│   2. 开发 + 测试                                                             │
│      cargo test --all-features                                              │
│      cargo clippy --all-features                                            │
│                                                                              │
│   3. 提交 PR                                                                 │
│      gh pr create --base alpha --head feature/xxx                           │
│                                                                              │
│   4. 关联 Issue                                                              │
│      在 PR 描述中引用: Fixes #issue-number                                   │
│                                                                              │
│   5. 等待人工审核                                                            │
│      至少 1 个人工 approve                                                   │
│      CI 全部通过                                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 禁止：

```
❌ 直接推 baseline
❌ 直接推 release
❌ 跳过 PR 直接合并
❌ 自行 approve 自己的 PR
```

---

## 3. 多 Agent 并行策略

```
alpha
  ├── feature/parser          # Agent A: 解析器改进
  ├── feature/executor        # Agent B: 执行器优化
  ├── feature/optimizer       # Agent C: 查询优化
  └── feature/network         # Agent D: 网络层改进
```

**原则**：每个 Agent 独立分支开发，避免冲突。

---

## 4. Agent 冲突解决规则

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        冲突解决规则                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ❌ 不允许自动 force push                                                   │
│                                                                              │
│   ✅ 必须 rebase 到最新 alpha                                                │
│      git fetch origin alpha                                                 │
│      git rebase origin/alpha                                                │
│                                                                              │
│   ✅ 冲突必须人工 review                                                     │
│      冲突文件需人工确认                                                      │
│      记录冲突解决原因                                                        │
│                                                                              │
│   ✅ 冲突解决后重新测试                                                      │
│      cargo test --all-features                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 安全策略

### 禁止 AI 执行：

```bash
# 禁止的危险操作
❌ git push -f origin baseline
❌ git push -f origin release
❌ git reset --hard baseline
❌ git reset --hard release
❌ git branch -D release/*
❌ git tag -d v* (删除正式标签)
❌ git push --delete origin v* (删除远程标签)
```

### 允许的安全操作：

```bash
# 允许的安全操作
✅ git push origin feature/xxx
✅ git tag v1.0.0-alpha.x (仅 alpha 标签)
✅ git branch -d feature/xxx (本地清理)
✅ gh pr create (创建 PR)
✅ gh pr comment (评论 PR)
```

---

## 6. CI 门禁机制

### 必须有的检查：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CI 门禁检查                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌───────────────┐     ┌───────────────┐     ┌───────────────┐            │
│   │   单元测试    │ ──► │   编译检查    │ ──► │    Lint       │            │
│   │  cargo test   │     │  cargo build  │     │  cargo clippy │            │
│   └───────────────┘     └───────────────┘     └───────────────┘            │
│           │                     │                     │                      │
│           ▼                     ▼                     ▼                      │
│        必须通过              必须通过              必须通过                  │
│                                                                              │
│   ┌───────────────┐     ┌───────────────┐     ┌───────────────┐            │
│   │   格式检查    │ ──► │   静态分析    │ ──► │   覆盖率      │            │
│   │  cargo fmt    │     │   安全扫描    │     │  llvm-cov     │            │
│   └───────────────┘     └───────────────┘     └───────────────┘            │
│           │                     │                     │                      │
│           ▼                     ▼                     ▼                      │
│        必须通过              必须通过              目标 ≥80%                │
│                                                                              │
│   ⚠️ CI 不通过禁止合并                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 版本推进审批流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        版本推进审批流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   Alpha 冻结                                                                 │
│   ├── AI 提交完成报告                                                       │
│   ├── 人工审核确认                                                          │
│   └── 打 Tag: vX.Y.Z-alpha                                                  │
│         │                                                                    │
│         ▼                                                                    │
│   Beta 审核                                                                  │
│   ├── AI 提交验证报告                                                       │
│   ├── 人工审核确认                                                          │
│   └── 打 Tag: vX.Y.Z-beta                                                   │
│         │                                                                    │
│         ▼                                                                    │
│   Release 批准                                                               │
│   ├── AI 提交测试报告                                                       │
│   ├── 人工审核确认                                                          │
│   └── 打 Tag: vX.Y.Z-rc.N                                                   │
│         │                                                                    │
│         ▼                                                                    │
│   Baseline 升级                                                              │
│   ├── 人工最终确认                                                          │
│   ├── 合并到 baseline                                                       │
│   └── 打 Tag: vX.Y.Z                                                        │
│                                                                              │
│   ⚠️ 每个阶段需人工确认，AI 不能自行推进                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 8. Agent 身份标识

| Agent | GitHub 身份 | 模型 | 职责 |
|:------|:------------|:-----|:-----|
| Codex | yinglichina8848 | GPT-4 | 架构设计、门禁审核 |
| Claude Code | sonaheartopen | Claude | 功能开发、测试 |
| OpenCode | sonaopenheart | OpenAI | 文档、流程 |
| TRAE | yinglichina | GLM-5.0 | 文档、协调 |

---

## 9. 通信规范

### Issue 评论格式：

```markdown
## [任务标题]

**发布者**: [Agent名称] ([模型])
**GitHub 身份**: [GitHub ID]
**时间**: [时间戳]

### 内容
...

*本报告由 [Agent名称] ([模型]) 发布*
```

### PR 描述格式：

```markdown
## 变更说明

### 关联 Issue
Fixes #xxx

### 变更内容
- ...

### 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

### 检查清单
- [ ] 代码风格符合规范
- [ ] 无安全漏洞
- [ ] 文档已更新
```

---

## 10. 异常处理

### AI 遇到冲突时：

1. 停止当前操作
2. 记录冲突详情
3. 请求人工协助
4. 等待人工确认后继续

### AI 遇到权限问题时：

1. 不要尝试绕过权限
2. 记录需要的操作
3. 在 Issue 中请求人工执行
4. 等待人工处理

---

## 🔥 最终效果

拥有此规范后，项目具备：

| 文档 | 作用 |
|:-----|:-----|
| `VERSION_PROMOTION_SOP.md` | 标准版本推进 SOP |
| `VERSION_FLOW_DIAGRAM.md` | 可视化分支架构图 |
| `AI_AGENT_COLLAB_GOVERNANCE.md` | AI 协作治理规范 |

这已经是 **"小型产品级工程治理框架"**。

---

*本文档由 TRAE (GLM-5.0) 创建*
