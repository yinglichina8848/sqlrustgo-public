gate_report.json
verification_report.json
audit_report.json
test_report.json
SOAK_*_REPORT.md
test_report.log
__pycache__/
*.pyc
*.profraw
target/
data/tpch-sf01-generated/
data/tpch-sf01/
artifacts/coverage/
artifacts/bench/
artifacts/gate/
.DS_Store
Cargo.lock
.worktrees/
v1.0.0/api-doc/*
# GitNexus tool artifacts (auto-injected, not for tracking)
.gitnexus/
CLAUDE.md
.claude/skills/

# Server LOAD DATA side effects: .json (table metadata dumps) and .wal (WAL files) emitted into data_dir at runtime
tests/data/tpch-sf001/*.json
tests/data/tpch-sf001/*.wal
tests/data/tpch-sf01/*.json
tests/data/tpch-sf01/*.wal

# Stability test output directories (timestamps appended) — not for tracking
test_results/

# REPL / mysql-server default data_dir (runtime side effects: .json table dumps + .wal)
.sqlrustgo/

# TPC-H .tbl fixture data (generated by scripts/gate/generate_sf001_fixture.py)
# Per AGENTS.md: "TBL 等基准数据 must NOT be committed directly"

# === Added in soak-p0 cleanup (2026-06-24) ===

# TPC-H raw tables (root data/) — loaded at test time, not source
data/*.tbl

# Tiny TPC-H fixture (sub-sf1) — also runtime-generated
tests/data/tpch-tiny/

# Integration test data
tests/integration/data/

# Test registry runtime data
crates/test-registry/data/

# Coverage report outputs (tarpaulin HTML)
coverage-report/

# Build artifact outputs
releases/
artifacts/releases/
artifacts/usage/
artifacts/reports/

# Runtime trace artifacts (TTrace files emitted by proof harnesses)
docs/formal/*TTrace*.bin
docs/formal/*_TTrace_*.bin

# Usage summary build outputs
scripts/usage/*.xlsx

# Release-time log dumps
docs/releases/*/logs/

# Worktree-local operational docs (mission complete, rebase plan etc.)
REBASE_AND_PR_PLAN.md
# Build output staging dir
release-artifacts/