//! sqlrustgo-bench-cli — **DEPRECATED** since v3.8.0.
//!
//! Use `sqlrustgo-mysql-server bench` instead. Full feature
//! parity migrates in a follow-up; the canonical binary prints
//! a helpful message today.

fn main() {
    eprintln!(
        "sqlrustgo-bench-cli: DEPRECATED since v3.8.0 — \
         use `sqlrustgo-mysql-server bench` instead"
    );
}
