[package]
name = "transaction-stress"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
thiserror = "1.0"
log = "0.4"
parking_lot = "0.12"

[dev-dependencies]
sqlrustgo = { path = "../.." }
sqlrustgo-storage = { path = "../storage" }
