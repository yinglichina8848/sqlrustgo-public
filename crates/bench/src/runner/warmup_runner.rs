//! Warmup runner for pre-heating the system

/// Warmup runner - pre-heats the system before benchmarking
pub struct WarmupRunner;

impl WarmupRunner {
    pub fn new() -> Self {
        Self
    }
}
