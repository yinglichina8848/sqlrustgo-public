//! Concurrent runner for executing benchmark workloads

/// Concurrent runner - executes workloads in parallel
pub struct ConcurrentRunner;

impl ConcurrentRunner {
    pub fn new() -> Self {
        Self
    }
}
