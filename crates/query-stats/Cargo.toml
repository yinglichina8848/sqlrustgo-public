[package]
name = "query-stats"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
parking_lot = "0.12"
chrono = "0.4"

[dev-dependencies]
sqlrustgo = { path = "../.." }
