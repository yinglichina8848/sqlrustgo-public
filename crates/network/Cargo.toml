[package]
name = "sqlrustgo-network"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1", features = ["full"] }
tower = "0.5"
serde = { version = "1", features = ["derive"] }
anyhow = "1"

[build-dependencies]
tonic-build = "0.12"
