[package]
name = "sqlancer"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
thiserror = "1.0"
log = "0.4"

[dev-dependencies]
sqlrustgo = { path = "../.." }
sqlrustgo-parser = { path = "../parser" }
sqlrustgo-storage = { path = "../storage" }
