//! sqlrustgo-tools — **DEPRECATED** since v3.8.0.
//!
//! Use `sqlrustgo-mysql-server diag` instead. Full feature
//! parity (catalog dump, table inspection, etc.) migrates in a
//! follow-up.

fn main() {
    eprintln!(
        "sqlrustgo-tools: DEPRECATED since v3.8.0 — \
         use `sqlrustgo-mysql-server diag` instead"
    );
}
