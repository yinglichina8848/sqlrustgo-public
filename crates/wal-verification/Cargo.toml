[package]
name = "sqlrustgo-wal-verification"
version = "0.1.0"
edition = "2021"
description = "WAL Formal Verification specifications"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[tlaplus]
version = "1.8.0"

[dev-dependencies]
tempfile = "3.14"
sqlrustgo-storage = { path = "../storage" }
